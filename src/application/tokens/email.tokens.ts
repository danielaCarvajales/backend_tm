/** Token de inyección para EmailSenderPort (string literal solicitado). */
export const EMAIL_SENDER_PORT = 'EmailSenderPort';

/** Token para el puerto de plantillas (simetría con el envío). */
export const EMAIL_TEMPLATE_PORT = 'EmailTemplatePort';
